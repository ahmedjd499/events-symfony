{% extends 'base.html.twig' %}

{% block title %}New Registration{% endblock %}

{% block body %}
<div class="container">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="d-flex align-items-center mb-4">
                <a href="{{ path('app_inscription_index') }}" class="btn btn-outline-secondary me-3">
                    <i class="bi bi-arrow-left"></i>
                </a>
                <h1 class="h2 text-primary mb-0">
                    <i class="bi bi-plus-circle"></i> New Registration
                </h1>
            </div>

            {{ include('inscription/_form.html.twig') }}
        </div>
    </div>
</div>
{% endblock %}
